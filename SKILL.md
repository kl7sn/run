---
name: run
description: Agent workflow skill. Auto-advance engineering tasks with durable markdown state in a configurable workspace folder. Document language via /run lang en|zh. Bundles skills/up for /run review maintenance. Triggers: /run, /run auto, /run init, /run new, /run bind, /run lang, /run review.
---

# /run

Install packaged skills from this repo:

- [`skills/run/SKILL.md`](skills/run/SKILL.md) — process protocol
- [`skills/up/SKILL.md`](skills/up/SKILL.md) — skill maintenance (`/run review` phase 2)

```bash
./install.sh all
```

Quick start: see [README.md](README.md).
